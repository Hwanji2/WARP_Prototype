using UnityEngine;

public class ex : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello worl1!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
